# Mini-Datadog 项目复盘报告

## 📌 项目概述

**项目名称**: Mini-Datadog 日志监控系统
**开发周期**: 虚拟实习项目
**技术栈**: Java Spring Boot + Python + H2 + HTML/JS

---

## 🔥 三个核心技术难点

### 难点一：日志文件的增量读取

#### 问题描述
LogAgent需要持续监控日志文件，只读取新增的内容，不能重复读取已处理的行。

#### 解决方案

```java
// 使用RandomAccessFile + 文件位置标记
private long filePosition = 0;

private void checkAndReadFile(File file) throws IOException {
    long currentLength = file.length();

    // 检测文件截断（logrotate场景）
    if (currentLength < filePosition) {
        filePosition = 0;  // 从头开始
    }

    if (currentLength > filePosition) {
        try (RandomAccessFile raf = new RandomAccessFile(file, "r")) {
            raf.seek(filePosition);  // 定位到上次位置
            String line;
            while ((line = raf.readLine()) != null) {
                processLine(line);
            }
            filePosition = raf.getFilePointer();  // 更新位置
        }
    }
}
```

#### 技术要点
1. **RandomAccessFile** 支持随机访问，可以seek到任意位置
2. **文件位置标记** 记录已读取的字节偏移量
3. **文件截断检测** 处理logrotate等场景
4. **编码转换** readLine()默认ISO-8859-1，需转UTF-8

#### 为什么不用WatchService？
- Java的WatchService在不同平台表现不一致
- 某些系统上有明显延迟
- 轮询方式更可控、更可靠

---

### 难点二：告警防抖动机制

#### 问题描述
错误率超过阈值时需要告警，但如果错误持续，不能一直重复告警（告警风暴）。

#### 解决方案

```java
private volatile boolean isAlerting = false;
private LocalDateTime lastAlertTime;
private static final int COOLDOWN_SECONDS = 60;

public void checkAndAlert() {
    double errorRate = calculateErrorRate();

    if (errorRate >= threshold) {
        // 检查冷却时间
        if (lastAlertTime != null &&
            lastAlertTime.plusSeconds(COOLDOWN_SECONDS).isAfter(LocalDateTime.now())) {
            return;  // 冷却中，跳过
        }

        triggerAlert(errorRate);
        isAlerting = true;
        lastAlertTime = LocalDateTime.now();

    } else if (isAlerting) {
        // 恢复通知
        resolveAlert(errorRate);
        isAlerting = false;
    }
}
```

#### 技术要点
1. **状态机** 用isAlerting标记当前是否在告警状态
2. **冷却时间** 60秒内不重复告警
3. **恢复通知** 错误率下降后发送恢复消息
4. **volatile关键字** 保证多线程可见性

#### 真实系统还需要什么？
- 多级别阈值（警告/严重/致命）
- 告警聚合和去重
- 告警确认和静默
- 多渠道通知（邮件/短信/钉钉）

---

### 难点三：前端实时数据更新

#### 问题描述
仪表盘需要实时展示最新数据，如何在不刷新页面的情况下更新图表和列表？

#### 解决方案

```javascript
// 定时轮询 + 增量更新
const REFRESH_INTERVAL = 5000;

document.addEventListener('DOMContentLoaded', function() {
    initCharts();  // 初始化图表实例
    refreshData(); // 首次加载
    setInterval(refreshData, REFRESH_INTERVAL);  // 定时刷新
});

async function refreshData() {
    // 并行请求
    const [stats, logs] = await Promise.all([
        fetch('/api/stats').then(r => r.json()),
        fetch('/api/logs').then(r => r.json())
    ]);

    // 增量更新（不重建图表，只更新数据）
    levelChart.data.datasets[0].data = [
        stats.errorCount, stats.warnCount, stats.infoCount
    ];
    levelChart.update();
}
```

#### 技术要点
1. **定时轮询** setInterval定期请求数据
2. **增量更新** 只更新数据，不重建DOM/图表
3. **并行请求** Promise.all提高效率
4. **Chart.js** update()方法高效更新图表

#### 为什么不用WebSocket？
- 轮询实现简单，适合5秒级刷新
- WebSocket有连接管理开销
- 对于演示项目，轮询足够

#### 优化方向
- WebSocket实现真正的实时推送
- 增量数据传输（只传变化的部分）
- 虚拟滚动处理大量日志

---

## 💼 简历写法建议

### 项目经历写法

```
Mini-Datadog 日志监控系统                              2024.XX - 2024.XX

• 设计并实现了完整的日志监控链路，包括日志采集、解析存储、统计分析、告警通知
• 使用RandomAccessFile实现日志文件增量读取，支持文件截断检测和断点续传
• 实现了基于滑动窗口的错误率统计和带防抖动的告警机制
• 使用Spring Boot + H2 + Chart.js构建可视化仪表盘，支持5秒级实时刷新

技术栈：Java Spring Boot, Python, H2, REST API, Chart.js
```

### 亮点提炼

1. **完整链路** - 不是只写一个模块，而是整个系统
2. **技术细节** - 增量读取、防抖动等体现深度
3. **数据支撑** - 5秒刷新、滑动窗口等具体数字
4. **技术选型** - 列出完整技术栈

---

## 🎤 面试介绍话术

### 30秒版本

> 我做了一个日志监控系统，类似简化版的Datadog。
>
> 它包括四个部分：Python的日志生成器、Java的日志采集Agent、Spring Boot的后端服务、还有一个可视化仪表盘。
>
> 主要解决了三个技术问题：文件增量读取、告警防抖动、前端实时更新。
>
> 这个项目让我对日志监控系统的架构有了完整的理解。

### 1分钟版本

> 这是一个完整的日志监控系统，我从零开始设计和实现。
>
> **架构设计**：采用四层架构。Python模拟器生成日志，Java Agent监控文件变化并采集，Spring Boot服务端解析存储和统计，前端仪表盘可视化展示。
>
> **核心挑战**：
> 1. 日志采集的增量读取，用RandomAccessFile配合位置标记实现
> 2. 告警的防抖动机制，避免告警风暴
> 3. 前端的实时更新，用定时轮询加Chart.js增量更新
>
> **技术选型**：选H2是因为开箱即用适合演示，选轮询而非WebSocket是因为5秒刷新足够，选Spring Boot是因为生态完善。
>
> 通过这个项目，我理解了监控系统的完整链路，也能和面试官讨论生产级系统需要哪些优化。

---

## 📈 可以延伸的优化方向

1. **性能优化**
   - 批量写入数据库
   - 引入Redis缓存热点数据
   - 日志压缩传输

2. **高可用**
   - 多Agent负载均衡
   - Server集群部署
   - 消息队列解耦

3. **功能增强**
   - 日志全文搜索（Elasticsearch）
   - 链路追踪（TraceID）
   - 多维度聚合分析

4. **运维能力**
   - Prometheus指标暴露
   - Grafana看板
   - Docker部署

面试时可以说：**"目前是单机演示版，如果要生产部署，我会考虑这些优化..."**
